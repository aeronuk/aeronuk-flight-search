<?xml version="1.0" encoding="UTF-8"?>

<!-- https://phpunit.readthedocs.io/en/latest/configuration.html -->
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         colors="true"
         failOnDeprecation="true"
         failOnNotice="true"
         failOnWarning="true"
         bootstrap="tests/bootstrap.php"
         cacheDirectory=".phpunit.cache"
>
    <php>
        <ini name="display_errors" value="1" />
        <ini name="error_reporting" value="-1" />
        <server name="APP_ENV" value="test" force="true" />
        <server name="SHELL_VERBOSITY" value="-1" />
    </php>

    <testsuites>
        <testsuite name="Project Test Suite">
            <directory>tests</directory>
        </testsuite>
    </testsuites>

    <source ignoreSuppressionOfDeprecations="true"
            ignoreIndirectDeprecations="true"
            restrictNotices="true"
            restrictWarnings="true"
    >
        <include>
            <directory>src</directory>
        </include>

        <exclude>
            <!-- Auto-generated Doctrine migrations and seed-data fixtures
                 aren't run through phpunit — migrations run as a separate
                 `doctrine:migrations:migrate` process, and fixtures via
                 `doctrine:fixtures:load` — so pcov never instruments them.
                 Both already run for real on every container start and
                 before every test pass, but that happens outside phpunit,
                 not via tests against this code. Excluding them keeps the
                 coverage denominator scoped to code this repo actually
                 tests, consistently with `phpstan.neon.dist`'s and
                 `phpcs.xml.dist`'s existing `src/Infra/Migrations`
                 exclusions for the same "never covered, keep it that way"
                 reasoning. -->
            <directory>src/Infra/Migrations</directory>
            <directory>src/Infra/DataFixtures</directory>
        </exclude>

        <deprecationTrigger>
            <method>Doctrine\Deprecations\Deprecation::trigger</method>
            <method>Doctrine\Deprecations\Deprecation::delegateTriggerToBackend</method>
            <function>trigger_deprecation</function>
        </deprecationTrigger>
    </source>

    <extensions>
    </extensions>
</phpunit>
